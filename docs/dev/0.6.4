0.6.4a: 
- [x] Add the config initializer to automatically copy files from .leonardo/instance.json into the Rails env. 

0.6.4b: 
- [x] Fix chromium not being in the base image

0.6.4c:
- [x] Puma root owned UID issue - Dockerfile entryway to do a setfacl to allow uid 1000 to access all root owned files that get generated by our LlamaPress-Simple docker container (less permission issues).

0.6.4d: 
- [x] Puma root owned UID issue - node_modules setfacl -R takes forever while is changes file permissions on tens of thousands of files. (in Leonardo)
- [x] Implement Devise trackable in the table. (in Leonardo)

0.6.4e: 
- [] Make the default scaffolding commands create tables with filter/search and left-hand side panels for show/edit/new forms.
- [] Add Roo XLS in the Gem.
